<h1 class="text-light text-center"><span class="font-bold text-primary font-weight-bold">Twittear</span></h1>
<div class="container px-5">
<%= form_with(model: [@user,tweet], local: true) do |form| %>
  <% if tweet.errors.any?%>
    <div id="error_explanation">
      <h2><%= pluralize(tweet.errors.count, "error") %> prohibited this tweet from being saved:</h2>

      <ul>
      <% tweet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
  
    <%= form.text_area :content, size: "70x5", class:'form-control form-control-lg', placeholder: "What's new?"%>
  </div>

  <div class="actions text-right">
    <%= form.submit "Twittear", :class => 'btn btn-primary' %>
  </div>
<% end %>
</div>